import React, { ReactElement } from 'react';
import WPSButtonTemplateTest from './button';

const Page = (): ReactElement => {
  return <WPSButtonTemplateTest text={'Default'} />;
};

export default Page;
